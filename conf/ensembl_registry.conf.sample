use strict;

use Bio::EnsEMBL::DBSQL::DBAdaptor;
use Bio::EnsEMBL::Utils::ConfigRegistry;

new Bio::EnsEMBL::DBSQL::DBAdaptor(
  -host    => 'localhost',
  -pass    => '@PASS@',
  -user    => '@USER@',
  -group   => 'core',
  -driver => 'mysql',
  -dbname  => '@DBNAME@'
);


1;
